@model WebApplication1.Models.Burden
@{
    ViewBag.Title = "Добавление новой нагрузки";
}

<h2>@ViewBag.Title</h2>

@using (Html.BeginForm("CreateBurden", "Burden", FormMethod.Post))
{
<fieldset>
    <div class="form-group">
        @Html.LabelFor(m => m.Name, "Название", new { @class = "col-md-2 control-label" })
        <div class="col-md-10">
            @Html.EditorFor(m => m.Name, "Наименование", new { @class = "form-control" })
        </div>
    </div>
    <br />
    <div class="form-group">
        <div class="col-md-offset-2 col-md-10">
            <input type="submit" class="btn btn-default" value="Создать" />
        </div>
    </div>
</fieldset>
}
@section Scripts {
    @Scripts.Render("~/bundles/jqueryval")
}